[pytest]
testpaths = tests
# starlette 1.6 emits a deprecation warning about an anyio alias from its own
# testclient module; it is library-internal, so keep the suite output clean.
filterwarnings =
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning:starlette.testclient