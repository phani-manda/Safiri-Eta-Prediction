# Pinned to the exact versions this project was built and validated against.
# Unpinned, a fresh clone can pick up a newer pandas or scikit-learn whose
# changed defaults shift the reported metrics -- which would make the numbers in
# report/ unreproducible without anyone noticing.
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
joblib==1.6.0
pytest==9.1.1
matplotlib==3.11.1
# Starlette>=1.0's TestClient needs the httpx2 HTTP client to run the API tests.
httpx2==2.12.0
